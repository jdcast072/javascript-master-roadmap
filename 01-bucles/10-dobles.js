// Ejercicio 10: Crea un nuevo arreglo con el doble de cada número.
const misNumeros = [1, 2, 3, 4, 5];
const numerosDobles = [];

for (const numero of misNumeros) {
  numerosDobles.push(numero * 2);
}

console.log(numerosDobles);
